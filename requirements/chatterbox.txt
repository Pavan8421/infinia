# Resemble AI Chatterbox — English zero-shot voice cloning compare.
#
# CRITICAL (Lightning + RTX PRO 6000 / sm_120 Blackwell):
# chatterbox-tts may pull torch 2.6+cu124, which has NO kernels for sm_120.
# After installing chatterbox, restore Studio CUDA torch:
#   pip install torch==2.8.0 torchvision==0.23.0 torchaudio==2.8.0 \
#     --index-url https://download.pytorch.org/whl/cu128
#
# Verify:
#   python -c "import torch; print(torch.__version__, torch.cuda.get_device_capability(0))"
# Expect: 2.8.0+cu128 and (12, 0) without "no kernel image" errors.
#
# Also conflicts with Parler (transformers==4.46.1). Hindi Parler metrics are locked.
chatterbox-tts==0.1.7
