# Lightning AI blocks creating additional venvs/conda envs in this Studio.
# Use the default `cloudspace` environment:
#   pip install -r requirements/base.txt
# Model-family pins live in requirements/{xtts,parler,cosyvoice,eval}.txt
